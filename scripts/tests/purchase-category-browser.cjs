/* eslint-disable @typescript-eslint/no-require-imports */
const assert=require('node:assert/strict');
module.exports=async({page,url,out,translate})=>{
 let scenarios=0;
 for(const[width,locale]of[[390,'th'],[1440,'th'],[1440,'en']]){
  const t=k=>translate(locale,'expenses.'+k),dialog=page.getByRole('dialog');await page.setViewportSize({width,height:1050});
  await page.goto(`${url}/finance/expenses?purchase060=1&locale=${locale}`);await page.getByRole('button',{name:t('new'),exact:true}).click();
  const combo=dialog.locator('#purchase-category');await combo.click();const count=await dialog.getByRole('listbox').getByRole('option').count();assert.ok(count>30);assert.equal(await dialog.getByRole('listbox').getByRole('option').last().innerText(),locale==='th'?'อื่น ๆ':'Other');
  await combo.fill('ศาล');assert.equal(await dialog.getByRole('listbox').getByRole('option').count(),2);await combo.press('Enter');assert.equal(await combo.inputValue(),locale==='th'?'ค่าธรรมเนียมศาล':'Court fees');
  await combo.click();assert.equal(await dialog.getByRole('listbox').getByRole('option',{selected:true}).innerText(),await combo.inputValue()|| (locale==='th'?'ค่าธรรมเนียมศาล':'Court fees'));
  await combo.fill('OFFICE SUPPLIES');assert.equal(await dialog.getByRole('listbox').getByRole('option').count(),2);await combo.press('ArrowDown');await combo.press('ArrowUp');await combo.press('Enter');assert.equal(await combo.inputValue(),locale==='th'?'วัสดุสำนักงาน':'Office supplies');
  await dialog.locator('#purchase-vendor').fill('Synthetic supplier');await dialog.locator('#purchase-amount').fill('300');await dialog.locator('#purchase-description').fill('Synthetic category test');await dialog.getByRole('button',{name:t('addThisItem'),exact:true}).click();
  await dialog.getByRole('button',{name:t('addItem'),exact:true}).click();await combo.fill('unlisted category');await dialog.locator('#purchase-vendor').fill('Synthetic supplier');await dialog.locator('#purchase-amount').fill('100');await dialog.locator('#purchase-description').fill('Custom category test');await dialog.getByRole('button',{name:t('addThisItem'),exact:true}).click();assert.equal(await dialog.locator('[data-request-item]').count(),1);
  await combo.click();await dialog.getByRole('listbox').getByRole('option',{name:locale==='th'?'อื่น ๆ':'Other',exact:true}).click();const custom=dialog.locator('#purchase-custom-category');assert.equal(await custom.getAttribute('required'),'');await custom.fill('หมวดทดสอบ');await dialog.getByRole('button',{name:t('addThisItem'),exact:true}).click();
  await dialog.getByRole('button',{name:t('saveForLater'),exact:true}).click();await dialog.getByRole('button',{name:t('editRequest'),exact:true}).click();assert.equal(await dialog.locator('[data-request-item]').count(),2);await dialog.getByRole('button',{name:t('editItem'),exact:true}).first().click();await combo.click();assert.equal(await dialog.getByRole('listbox').getByRole('option',{selected:true}).innerText(),locale==='th'?'วัสดุสำนักงาน':'Office supplies');assert.equal(await dialog.evaluate(e=>e.scrollWidth>e.clientWidth+1),false);await page.screenshot({path:out+`/purchase-category-${locale}-${width}.png`});await combo.press('Escape');await dialog.getByRole('button',{name:t('saveItemChanges'),exact:true}).click();await dialog.getByRole('button',{name:t('editItem'),exact:true}).last().click();assert.equal(await custom.inputValue(),'หมวดทดสอบ');
  const saved=await page.evaluate(()=>window.calls.find(c=>c.name==='save_finance_expense_request').args.p_items);assert.equal(saved[0].input.category,'company.supplies');assert.equal(saved[1].input.category,'หมวดทดสอบ');scenarios++;
  await page.goto(`${url}/finance/expenses?purchase060=1&locale=${locale}&scenario=draft&historicalClaim=1`);await page.locator('[data-request-row] button').click();await dialog.getByRole('button',{name:t('editRequest'),exact:true}).click();await dialog.getByRole('button',{name:t('editItem'),exact:true}).first().click();await combo.click();assert.equal(await dialog.getByRole('listbox').getByRole('option',{selected:true}).innerText(),locale==='th'?'ค่าเดินทาง / ที่พัก':'Travel / Accommodation');assert.equal(await dialog.getByRole('listbox').getByRole('option').last().innerText(),locale==='th'?'อื่น ๆ':'Other');scenarios++;
  console.log('PASS company category',width,locale);
 }
 return scenarios;
};
